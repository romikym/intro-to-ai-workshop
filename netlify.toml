[build]
  command = "chmod +x node_modules/@esbuild/linux-x64/bin/esbuild 2>/dev/null || true; chmod +x node_modules/.bin/esbuild 2>/dev/null || true; npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  PUPPETEER_SKIP_DOWNLOAD = "true"

[dev]
  command = "npm run dev"
  port = 8888
  targetPort = 5173

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
